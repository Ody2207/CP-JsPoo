export default class Teacher {
    constructor({
        id,
        name,
        especiality,
    }) {
        this.id = id;
        this.name = name;
        this.especiality = especiality;
    }
}